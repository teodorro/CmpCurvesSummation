<UserControl x:Name="LabelChoose" x:Class="ProcessingModule.ProcessingCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProcessingModule"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Content="Текущая последовательность обработок" VerticalAlignment="Bottom" />
        <ListBox x:Name="ListBoxProcessing" Grid.Row="1" />
        <Label Content="Возможные процедуры обработки" Grid.Row="2" VerticalAlignment="Bottom"  />
        <ListBox x:Name="ListBoxChoose" Grid.Row="3" />

    </Grid>
</UserControl>
