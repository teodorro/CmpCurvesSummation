<UserControl x:Class="ProcessingModule.Views.ProcessingCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProcessingModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Текущая последовательность обработок" VerticalAlignment="Bottom" FontWeight="Bold" />
        <ListBox x:Name="ListViewChoose" Grid.Row="1" ItemsSource="{Binding ProcessingList}" AllowDrop="True" Drop="ListViewChoose_Drop" />
        <Label Content="Возможные процедуры обработки" Grid.Row="2" VerticalAlignment="Bottom" FontWeight="Bold"  />
        <ListBox x:Name="ListViewProcessing"  Grid.Row="3" ItemsSource="{Binding ChooseList}"  PreviewMouseMove="ListViewProcessing_PreviewMouseMove">
        </ListBox>
    </Grid>
</UserControl>
