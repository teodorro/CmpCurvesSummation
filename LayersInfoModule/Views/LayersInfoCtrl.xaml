<UserControl x:Class="LayersInfoModule.Views.LayersInfoCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LayersInfoModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Слои" VerticalAlignment="Bottom" FontWeight="Bold" />
        <DataGrid x:Name="LayersListDataGrid" Grid.Row="1" CanUserAddRows="False" CanUserSortColumns="False"
                  ItemsSource="{Binding Layers}">
            <!-- <DataGrid.ItemContainerStyle> -->
            <!--     <Style TargetType="DataGridRow"> -->
            <!--         <EventSetter Event="MouseDoubleClick" Handler="ProcessingListDataGrid_DoubleClick"></EventSetter> -->
            <!--     </Style> -->
            <!-- </DataGrid.ItemContainerStyle> -->
        </DataGrid>
    </Grid>
</UserControl>
